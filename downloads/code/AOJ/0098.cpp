#include <iostream>
#include <algorithm>
#include <cstdio>
#include <cstdlib>
#include <cmath>
#include <map>
#include <list>
#include <vector>
#include <stack>
#include <queue>
#include <string>
#include <set>

#define rep(i, a, b) for(int i = a; i < b; i++)

using namespace std;

int n;

int main(){
    cin >> n;
    vector<vector<int> > a(n);
    rep(i, 0, n){
        a[i] = vector<int>;
        rep(j, 0, n){
            cin >> a[i][j];
        }
    }
    
    rep(i, 0, n){
        
    }
}